package appconfig

import "testing"

// TestCheckCommandExists ensures that the proper path of a binary is returned
// if it exists
func TestCheckCommandExists(t *testing.T) {
}

// TestGetConfigDir ensures that a valid path is returned for the users ephemeral-iam
// configuration directory
func TestGetConfigDir(t *testing.T) {
}
