package gcpclient

import "testing"

// TestQueryTestablePermissionsOnResource ensures that the list of possible permissions
// that can be bound to a resource are properly fetched
func TestQueryTestablePermissionsOnResource(t *testing.T) {
}

// TestQueryComputeInstancePermissions ensures that the permissions granted to a user
// on a compute instance are properly determined
func TestQueryComputeInstancePermissions(t *testing.T) {
}

// TestQueryProjectPermissions ensures that the permissions granted to a user
// on a project are properly determined
func TestQueryProjectPermissions(t *testing.T) {
}

// TestQueryPubSubPermissions ensures that the permissions granted to a user
// on a Cloud Pub/Sub topic are properly determined
func TestQueryPubSubPermissions(t *testing.T) {
}

// TestQueryServiceAccountPermissions ensures that the permissions granted to a user
// on a service account are properly determined
func TestQueryServiceAccountPermissions(t *testing.T) {
}

// TestQueryStorageBucketPermissions ensures that the permissions granted to a user
// on a storage bucket are properly determined
func TestQueryStorageBucketPermissions(t *testing.T) {
}
